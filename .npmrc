# 0.18 is published under the `next` dist-tag. pnpm would auto-install colyseus'
# peer edges from `latest` (0.17.x) and build a mixed 0.17/0.18 tree; colyseus
# already ships them as ^0.18.1 dependencies. npm ignores this key.
auto-install-peers=false
